"""Pipeline scripts that produce results. Each file is a runnable script
with an `if __name__ == '__main__'` (or top-level body) and writes
artifacts under `data/` or `figures/`. Library code lives in `algebra/`,
`lean/`, `miu/`, and `config.py`.
"""
